# Debe usar el rol de aplicación (app_user), nunca el superusuario.
# Los valores reales viven en ../../.env (APP_DB_USER / APP_DB_PASSWORD / POSTGRES_PORT).
DB_HOST=127.0.0.1
DB_PORT=5433
DB_NAME=agenda_gober
DB_USER=app_user
DB_PASSWORD=changeme
PORT=3001
